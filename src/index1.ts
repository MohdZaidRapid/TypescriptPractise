// method 1
let a: string;
let nambo: number;
let check: boolean;

// method 2
let namboo = <string>"zaid";

// union
let surname: string | number;

surname = "46";

const func = (n: number, m: number): number => {
  console.log(n, m);
  return n * m;
};
